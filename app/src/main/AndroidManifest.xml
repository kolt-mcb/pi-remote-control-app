<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
      Cleartext is required for ws:// connections to a user's pi on the LAN
      — the whole product. We can't use network-security-config to scope it
      to RFC1918 because Android's <domain> matcher doesn't support CIDR
      ranges, and users always connect via literal IPs (192.168.x.y) which
      wouldn't match a domain prefix anyway. The mitigation lives at the
      UX layer: the user explicitly types/scans a URL, so they choose the
      host. README documents that public ws:// is dangerous and the
      extension supports an optional shared-secret token (PI_REMOTE_TOKEN)
      for cases where the host is exposed.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.PiRemoteControl">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PiRemoteControl">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PiService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- TestReceiver is registered only in src/debug/AndroidManifest.xml so
             release builds cannot be driven by external broadcasts. -->

    </application>

</manifest>
